name: personal_time_flutter
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: 1.4.1+1
  hive_flutter:
    git:
      url: https://github.com/hivedb/hive.git
      path: hive_flutter
  sqflite: 1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
